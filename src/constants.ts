import { TtrpgAudioManagerSettings } from './types'

export const ALLOWED_FILE_EXTENSIONS: string[] = ['mp3', 'wav', 'ogg']

export const DEFAULT_SETTINGS: TtrpgAudioManagerSettings = {
  audioFolders: [],
  playlists: [],
  scenes: [],
}
